name: Example2_Auth
options:
  bundleIdPrefix: com.ivan.lab8
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
targets:
  Example2_Auth:
    type: application
    platform: iOS
    sources:
      - path: Example2_Auth
    settings:
      base:
        INFOPLIST_FILE: Example2_Auth/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.ivan.lab8.Example2_Auth
        TARGETED_DEVICE_FAMILY: "1,2"
    info:
      path: Example2_Auth/Info.plist