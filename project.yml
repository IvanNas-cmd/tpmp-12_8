name: Example4_MapKit
options:
  bundleIdPrefix: com.ivan.lab8
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
targets:
  Example4_MapKit:
    type: application
    platform: iOS
    sources:
      - path: Example4_MapKit
    settings:
      base:
        INFOPLIST_FILE: Example4_MapKit/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.ivan.lab8.Example4_MapKit
        TARGETED_DEVICE_FAMILY: "1,2"
    info:
      path: Example4_MapKit/Info.plist
    preBuildScripts:
      - name: "Link Example4_MapKit"
        script: ""
    dependencies:
      - sdk: MapKit.framework
      - sdk: CoreLocation.framework